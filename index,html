<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Moja Szafa Prototype</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 20px; }
    header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
    .styles { display: flex; gap: 10px; margin-bottom: 20px; overflow-x: auto; }
    .styles button {
      padding: 10px 15px;
      border: none;
      background-color: #ddd;
      border-radius: 5px;
      cursor: pointer;
    }
    .styles button.selected {
      background-color: #aaa;
      color: #fff;
    }
    .grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; margin-bottom: 20px; }
    .grid div { background: #eee; height: 100px; border-radius: 5px; }
    .action { display: flex; gap: 10px; }
    .action button {
      flex: 1;
      padding: 15px;
      border: none;
      background-color: #28a;
      color: white;
      border-radius: 5px;
      cursor: pointer;
      font-size: 1em;
    }
  </style>
</head>
<body>
  <header>
    <h1>Moja Szafa</h1>
    <div>
      <button>⚙️</button>
      <button>Pinterest 🔗</button>
    </div>
  </header>

  <div class="styles">
    <button onclick="selectStyle(this, 'Klasyczny')">Klasyczny</button>
    <button onclick="selectStyle(this, 'Boho')">Boho</button>
    <button onclick="selectStyle(this, 'Glamour')">Glamour</button>
    <button onclick="selectStyle(this, 'Cottagecore')">Cottagecore</button>
    <button onclick="selectStyle(this, 'Dark Academia')">Dark Academia</button>
  </div>

  <div class="grid">
    <div></div><div></div><div></div>
    <div></div><div></div><div></div>
  </div>

  <div class="action">
    <button onclick="recommendStyle()">Wybierz coś fajnego</button>
    <button onclick="recommendStyle(selectedStyle)">W stylu „<span id='styleLabel'>losowym</span>”</button>
  </div>

  <script>
    let selectedStyle = null;

    function selectStyle(btn, style) {
      document.querySelectorAll('.styles button').forEach(b => b.classList.remove('selected'));
      btn.classList.add('selected');
      selectedStyle = style;
      document.getElementById('styleLabel').innerText = style;
    }

    function recommendStyle(style) {
      if (!style) style = "losowym";
      alert("Proponowana stylizacja (" + style + "):\n1) Outfit główny\n2) Alternatywa 1\n3) Alternatywa 2");
    }
  </script>
</body>
</html>
